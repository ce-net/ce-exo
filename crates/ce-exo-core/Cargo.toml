[package]
name = "ce-exo-core"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "ce-exo shared core: hardware probe, model registry, the auto-mode (replica vs pipeline-split) placement planner, wire protocol, and capability abilities. Pure logic, no I/O — the substrate the worker/router/SDK build on."

[lib]
name = "ce_exo_core"
path = "src/lib.rs"

[dependencies]
anyhow.workspace = true
serde.workspace = true
serde_json.workspace = true
hex.workspace = true
toml.workspace = true
